# 把 DSH Native 放进启动台（手动、可重复）

这份文档记录**手动**做一次就能长期生效的完整流程，不需要 Xcode 每次编译、也不需要依赖
任何临时会话。所有命令都在 `harness-native/` 目录（即本文件所在目录）下执行。

---

## 0. 先搞懂为什么"在 Xcode 里 Run"永远进不了启动台

Xcode 的 ▶ Run 只会把产物写进 DerivedData：

```
~/Library/Developer/Xcode/DerivedData/NativeHarness-<hash>/Build/Products/Debug/DSHNative.app
```

而启动台（Launchpad）和聚焦（Spotlight）**只索引固定几个目录**：

| 会被索引 | 不会被索引 |
|---|---|
| `/Applications` | `~/Library/Developer/Xcode/DerivedData/...` |
| `~/Applications`（即 `/Users/<你>/Applications`） | `~/Documents/...`、`/tmp/...` |
| `/System/Applications` | 桌面、下载等任意目录 |

结论：**Xcode 里没有任何开关能"加进启动台"**。要进启动台，只有一条路 —— 把 `.app`
包放进上面左边那三个目录之一。这就是全部原理。

另外，从 Xcode 的 Run 里直接拷贝 DerivedData 里那个 Debug 包是不行的：

- Debug 构建开了 `ENABLE_DEBUG_DYLIB`，`DSHNative` 只是一个壳，真正代码在
  `Contents/MacOS/DSHNative.debug.dylib`，**只有 Xcode 启动它时才解析得到**；
- Debug 产物在 DerivedData 里，Xcode 一清理就没了。

所以正确做法是：**用 Release 构建出一个自包含的 App，再把它复制进 `~/Applications`。**

---

## 1. 最快的手动流程（3 条命令）

全部都是脚本，可重复执行：

```bash
cd /path/to/native-harness

# ① 构建 Release 版可安装产物（产物落在 /tmp/harness-native-build/DSHNative.app）
Tools/build.sh release

# ② 安装进 ~/Applications（免密码、启动台会索引）
Tools/build.sh install --no-build

# ③ 逐项验证（退出码 0 = 全部通过，可直接接在 && 后面）
Tools/build.sh verify
```

`Tools/build.sh verify` 会逐条打印检查结果，任何硬性失败都会让整条命令以非 0 退出：

```
checking /Users/<你>/Applications/DSHNative.app
  [ok]   bundle exists
  [ok]   executable present (arm64)
  [ok]   code signature verifies
  [ok]   app icon declared (AppIcon) and present
  [ok]   bundle id ai.deepseek.nativeharness.DSHNative
  [ok]   registered with LaunchServices — Launchpad will list it
all checks passed
```

然后打开启动台（触控板四指捏合，或 Dock 里的火箭图标），翻到应用页就能看到
**DSHNative**，点它即可启动。

- `Tools/build.sh install`（不带 `--no-build`）等于 ①+② 一条命令做完；
  再加 `--wait` 会等到 LaunchServices 真正索引到新包为止：
  ```bash
  Tools/build.sh install --wait
  ```
- 想装到全局的 `/Applications`（所有用户可见、Finder 的"应用程序"里也在）：
  ```bash
  Tools/build.sh install --system     # 会提示输入管理员密码
  ```
  为什么需要密码：这台机器的 `/Applications` 属主是 `root:admin`、权限 `drwxrwxr-x`
  且 admin 组**没有**写位，所以必须提权。`~/Applications` 不需要。
- 想顺便钉到程序坞：`Tools/build.sh install --dock`。

**以后每次改了代码**，重新跑一遍 `Tools/build.sh install` 即可覆盖旧版本，
启动台里还是同一个图标，不用重新添加。

---

## 1.5 让"插件管理/命令行工具"也能用（`Tools/build.sh shim`）

这个 App 是**下载 release 来运行 harness**的，从没往系统里装过全局 `dsh` 命令。后果是
任何需要调用 CLI 的功能都会失败，最典型的是 **设置 → 插件** 里的安装/卸载，报
`spawn dsh ENOENT`。

```bash
Tools/build.sh shim      # 把 Tools/dsh-cli.sh 装成 /opt/homebrew/bin/dsh
dsh --version            # 应打印当前 harness 版本，例如 0.1.5-rc.1
```

这个 shim 不写死版本：它运行时读 App 自己的 `installs.json` 拿 active release，
所以 App 升级后自动跟着换，不用重装 shim。也可以用 `DSH_APP_HOME` 指到别的
harness 目录：`DSH_APP_HOME=... dsh --version`。

---

## 1.6 运行时目录在哪、怎么搬（`Tools/relocate-root.sh`）

App 不会把 harness 装进自己的 `.app`：bundle 有签名封印，往里写就破坏签名。它把整棵树
放在**用户目录下一个自己拥有的 root** 里：

| | 路径 |
|---|---|
| 现在的默认 root | `~/.nativeharness` |
| 2026-09 之前的旧位置 | `~/Library/Application Support/NativeHarness` |
| 覆盖开关 | `NATIVE_HARNESS_ROOT`（App / `harnessctl` / console 都认）、`DSH_APP_HOME`（只影响 PATH 上的 `dsh` shim） |

root 里面：`harness/`（releases、`installs.json`、`current`、logs）、`home/`（就是交给
harness 的 `DSH_HOME`：profiles、sessions、settings、credentials）、`runtime/`（npm 缓存等）。

**升级前已经装过的机器默认继续用旧路径**，不会自动搬 —— 否则会看起来像 release、插件、
会话全没了。判定标准是"里面已经有 `harness/` 或 `home/`"：空目录不算数，所以误建的
`~/.nativeharness` 也不会把旧数据藏起来。要搬就用脚本。它会先退出 App（读 `server.json`
的 pid）、同卷原子 `mv`、并在旧路径留一个符号链接，所以树内那 500+ 条写死旧路径的符号链接
（`profiles/*/.dsh-module-fallback`）继续可用，不需要重装插件：

```bash
cd /path/to/native-harness
Tools/relocate-root.sh --dry-run     # 先看一遍，不动任何文件
Tools/relocate-root.sh               # 搬到 ~/.nativeharness（默认）
Tools/relocate-root.sh ~/NativeHarness   # 或搬到指定目录
```

回滚：

```bash
rm ~/Library/Application\ Support/NativeHarness && \
  mv ~/.nativeharness ~/Library/Application\ Support/NativeHarness
```

注意两点：**别删掉旧路径上那个符号链接**（删了 App 会在旧路径重新建一个空 root，看起来像
数据丢了）；另外 `Desktop`/`Documents`/`Downloads` 受 TCC 保护，别把 root 放那里。

---

## 2. 不用脚本、纯手工等价做法

如果哪天不想用 `Tools/build.sh`，等价操作是：

```bash
cd /path/to/native-harness

# 1) 重新生成 Xcode 工程（工程文件是生成出来的，新增源文件必须先跑这一步）
node Tools/gen-xcodeproj.mjs

# 2) Release 构建（注意：destination 不要写 arch=arm64，本机只报告 arm64e）
xcodebuild -project NativeHarness.xcodeproj -scheme DSHNative \
  -configuration Release \
  -derivedDataPath /tmp/dsh-build \
  -destination 'platform=macOS' \
  build

# 3) 确认 bundle 里有可执行文件（空的就是链接失败了，别往下走）
ls -l /tmp/dsh-build/Build/Products/Release/DSHNative.app/Contents/MacOS/DSHNative

# 4) 复制进 ~/Applications（用 ditto，普通 cp -R 会破坏签名封装）
ditto /tmp/dsh-build/Build/Products/Release/DSHNative.app ~/Applications/DSHNative.app

# 5) 刷新 LaunchServices 注册 + 去掉可能的隔离属性
/System/Library/Frameworks/CoreServices.framework/Frameworks/LaunchServices.framework/Support/lsregister \
  -f -R -trusted ~/Applications/DSHNative.app
xattr -dr com.apple.quarantine ~/Applications/DSHNative.app 2>/dev/null || true
touch ~/Applications/DSHNative.app
```

在 Xcode 图形界面里做同样的事：

1. 菜单 **Product → Scheme → Edit Scheme… → Run → Build Configuration 改成 `Release`**；
2. **Product → Build**；
3. 左侧导航最底部 **Products → DSHNative.app** 右键 → **Show in Finder**；
4. 把 Finder 里那个 `DSHNative.app` 拖到 **`~/Applications`**（Finder 里按 `⌘⇧H` 回主目录，
   没有 `Applications` 文件夹就自己新建一个，名字必须是 `Applications`）。

第 4 步之后**不需要**重启、不需要登出，启动台通常几秒内就会出现；偶尔慢的话见下一节。

---

## 3. 放好了但启动台里没出现 / 出现旧图标

按顺序做，基本都能解决：

```bash
# 让 LaunchServices 重新扫描（最关键的一步）
/System/Library/Frameworks/CoreServices.framework/Frameworks/LaunchServices.framework/Support/lsregister \
  -f -R -trusted ~/Applications/DSHNative.app

# 确认系统是否认得这个路径（应能看到 path: /Users/<你>/Applications/DSHNative.app）
/System/Library/Frameworks/CoreServices.framework/Frameworks/LaunchServices.framework/Support/lsregister -dump \
  | grep -A2 "^bundle id: *DSHNative"

# 图标缓存没刷新时：重启一下 Dock（图标不消失）
killall Dock

# 还不行：重启 Finder（桌面图标会重排）
killall Finder
```

按"症状"对号入座：

| 现象 | 原因 | 处理 |
|---|---|---|
| 启动台里完全没有 | 注册没刷新 / 不在被索引的目录 | 跑上面的 `lsregister -f -R -trusted`，确认路径在 `/Applications` 或 `~/Applications` |
| 双击没反应、像死了一样 | 拷的是 Release 链接失败留下的**空壳**（只有 Info.plist） | `ls Contents/MacOS/` 看有没有 `DSHNative`；没有就说明构建失败，重跑 `Tools/build.sh release` 看报错 |
| 一启动就崩 | 搬了 Debug 包（依赖 `DSHNative.debug.dylib`） | 改用 Release 产物 |
| 图标是白纸 | bundle 里没有图标资源 | 用 `Tools/build.sh release` 的产物（图标走 `Assets.xcassets`），不要手工拼 bundle |
| 图标还是旧的（改过图标后） | 图标缓存 | `touch ~/Applications/DSHNative.app && killall Dock` |
| 首次打开提示"无法验证开发者" | 极少数情况下带了隔离属性 | `xattr -dr com.apple.quarantine ~/Applications/DSHNative.app`（本机构建的包一般没这属性） |

验证签名是否完整（本机是 ad-hoc 签名，`Signature=adhoc` 属正常）：

```bash
codesign --verify --deep --strict ~/Applications/DSHNative.app && echo SIGN_OK
codesign -dv --verbose=2 ~/Applications/DSHNative.app 2>&1 | grep -E "Signature|Sealed Resources"
```

---

## 4. 为什么这套流程是"稳定"的（三个已踩过的坑）

这三个坑都已在工程里修掉，所以上面的命令可以直接重复跑：

1. **Release 必须只编 arm64。** 仓库里 vendored 的 `Vendor/zstd/lib/libzstd.a` 只有
   arm64 切片，而 Xcode 默认 Release 会同时编 `arm64 x86_64`，x86_64 那一遍链接必然
   失败（`Undefined symbols: _ZSTD_isError …`）。坑在于：**链接失败之后 xcodebuild 仍会
   拼出并签名一个只有 Info.plist 的 `DSHNative.app`** —— 双击毫无反应，也不报错。
   工程因此固定了 `ARCHS = $(NATIVE_ARCH_ACTUAL)`（见 `Tools/gen-xcodeproj.mjs`）。
2. **Release 必须关掉 Xcode 的预览 dylib**：`ENABLE_DEBUG_DYLIB = NO`，否则产物是依赖
   Xcode 的壳。
3. **bundle 要整体签名**：`Tools/build.sh release` 故意**不**传
   `CODE_SIGNING_ALLOWED=NO`，交给 xcodebuild 用 ad-hoc 身份签。带资源（图标）的包若只
   有可执行文件被链接器签名，`codesign --verify` 会报
   `code has no resources but signature indicates they must be present`，且没有
   `_CodeSignature/CodeResources`。

脚本还会在复制前**自己校验**：没有可执行文件或 `codesign --verify` 不过就直接报错退出，
不会把一个"看起来装了、其实双击没反应"的包放进 `~/Applications`。

---

## 5. 常用命令速查

```bash
Tools/build.sh release            # 只构建，产物：/tmp/harness-native-build/DSHNative.app
Tools/build.sh install            # 构建 + 装进 ~/Applications
Tools/build.sh install --no-build # 只装已有的构建产物
Tools/build.sh install --system   # 装进 /Applications（要管理员密码）
Tools/build.sh install --dock     # 顺便钉到程序坞
Tools/build.sh install --quit     # 覆盖前先退出正在运行的 DSH Native
Tools/build.sh clean              # 清掉构建缓存与已构建产物
```

`--quit` 的注意点：这个 App 会接管（杀掉旧进程）已运行的 harness，所以当有浏览器窗口
正由同一个 harness 提供服务时，退出它会连带关掉那个窗口。
